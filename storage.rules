rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Anyone can view the files
      allow read: if true;
      // Only the authenticated admin can upload/delete
      allow write: if request.auth != null;
    }
  }
}
